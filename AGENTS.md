# promicro-presence — エージェントへの指示

Pro Micro + HC-SR04 超音波センサーの在室検知システム。距離テレメトリを常時配信する
**常用固定デバイス**。配線・運用の詳細は README.md。

## シリアルポート・書込

- **固定ポート: `/dev/cu.usbmodemHIDPC1`** — 本プロジェクト専用
  （`config.sh` の `DEVICE`・距離テレメトリ `dist,signal` を常時配信中）
- **本プロジェクトの開発書込は可**: ビルド成功後、ユーザーが `sh upload.sh` で
  書き込む。エージェントは勝手に書き込まない（デバイス接続状態に依存するため）
- **他プロジェクトの実験・テスト目的でこのポートへ書込まない**: 実験・ファーム
  検証は開発共通ポート `/dev/cu.usbmodem212101` か別ボードで行うこと

## 自動ビルドルール（必須）

**重要:** Arduinoコード（`.ino`, `.cpp`, `.h`ファイル）を変更した場合、**必ず直後にビルドを実行すること。** コードを変更しただけではマイコンに書き込まれない。

### 手順

1. コードを変更する
2. **即座にビルドを実行**: `bash compile.sh`
3. ビルド結果をユーザーに報告する（成功・失敗問わず）

### ビルド結果の報告形式

**成功時:**
- ✅ ビルド成功
- Flash使用量 / RAM使用量を表示

**失敗時:**
- ❌ ビルド失敗
- エラーメッセージを表示
- 解決策を提示して修正

